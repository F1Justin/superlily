# Superlily 权威路线图

本文是当前唯一的实施路线。项目目标以根目录 [`MANIFESTO.md`](../MANIFESTO.md)
为最高约束；已经接受的 ADR、合同和生产验收只说明现有基础及其不变量，不会因为旧
Phase 编号自动产生下一项工作。

## 当前结论（2026-09-19）

当前生产仍是 Nekro Runtime；R2 前两阶段已部署到 `2.3.3-superlily.11-r2.1`，
精确镜像与源码见 [部署锁](../deploy/nekro-runtime.lock.yml)。本节取代下方历史发布记录中的
“当前/尚待”措辞；原始记录保留为事实证据，不自动生成待办。

**近期只围绕现有回答体验：让莉莉真正读用户要求分析的 PDF、按需补看图片，再回答，
需要排版时沿用渲染器。** 不为群聊 Agent、插件命令或新模型寻找人为场景。

| 编号 | 内容 | 当前状态 / 下一步 |
| --- | --- | --- |
| R0 | 冻结成功基线、统一路线入口 | 已完成，历史基线不是当前镜像 |
| R1 | 渲染失败反馈、修改重试、成功后发送 | 已完成、已上线；维护真实使用问题 |
| R1.1 | 引用焦点与引用图片连续性 | 已完成、已上线 |
| R1.2 | 节日皮肤及日期切换 | 已完成、已上线；真实节日效果作为运行观察，不作为未完成阶段 |
| R2 | 执行凭证、安全 RPC、群共享目录、任务临时区、导出与容量保护 | 前两阶段已完成、已上线；后续通用权限/投递扩展未做，不自动扩建 |
| R3 | 在现有 Runtime 中修复真实体验问题 | 方向项，不是另建通用群聊 Agent |
| R3.1 | 对话让步与按需 Web Search | 因成本暂停；代码与面板开关已有，生产关闭 |
| R3.2 | 真正阅读资料与按需看图 | 本次整理新增的需求编号；已调查、未实施，近期优先项 |
| R4 | 参考工程研究 | 仅按需支持具体改进，不独立排期、不引入新 Runtime |
| R5 | 需求触发的产品扩展 | 分类入口，不是自动开工清单 |
| R5.1 / C0-E | QQ 平台事实完整性 | 已实现、已上线；缺少个别事件样本作为覆盖备注 |
| R5.2 / C0-F | 通讯录与群成员快照 | 已完成、已上线 |
| R5.3 / C0-G | OneBot 平台副作用账本 | 已完成、已上线 |
| R5.4 / C0-H | 断线补洞 | 已实现、两端启用；保留平台不可恢复缺口及未测场景说明 |
| R5.5 / C0-I | 媒体归档与合并转发展开 | 代码已部署、功能未启用；不全量下载，不与按需阅读混为一项 |

**工作判定**：实现完成、关键链路可用就记录完成；技术验证是开发步骤，不额外挂“待验收”
项目。已知故障明确记为故障，未启用记为未启用；不把没测到某类事件说成没有做完，
也不把有限覆盖说成支持平台全量历史或所有外部权限。

- P1–P4 为已完成的基础；P5 Core Agent v1 为已完成的冻结参考，不替代 Nekro。
- H0–H4 历史统一已完成；C0 名称历史、后续 SQLite 旧记录导入均保留。
- 旧 P6–P11 已退出执行队列。更多账号、多平台、Memory/RAG、活动、Fumo/Live2D
  是需求或愿景，不因旧编号而开工。
- Luna/双模型未引入；讨论价格不等于接入决定。R3.1 不因本次整理恢复。
- Wolfram worker 本次只读检查仍退出，是已知运维故障，不是新阶段；本次未修复。
- [全部编号对照](ROADMAP_INDEX.md)列出 R、P/Phase、C0、H 及旧子阶段；重复名称只算同一项。
- [R0 冻结基线](R0_BASELINE.md)、各阶段合同与发布记录继续保留，不被当成最新生产快照。

## 运行时方向

Superlily 不采用某个现成 Agent Harness 作为预定终点。Pi、Codex、DSH 和其他优秀
Harness 只用于研究为什么自然执行、真实反馈、临时脚手架和可逆试错有效；项目把这些
原则转化为适合长期社交主体的自有运行时。它们不是候选 backend、部署依赖、shadow
实现或 A/B 对象。

当前生产实现就是 SuperLily 的 Nekro fork。近期没有“换 Runtime”任务，也不为尚未
存在的第二实现预建多后端框架。方向决策见
[`ADR 0019`](adr/0019-cognitive-runtime-direction.md)。

## R0：冻结当前成功基线（已完成）

R0 不改变运行行为。它完成三件事：

1. 钉住 P1–P5、H0–H4、数据库、Runtime 镜像/源码和 Nekro 成本事实；
2. 删除会继续指挥施工的旧路线、未来阶段预案和已被正式签署覆盖的中间文档；
3. 建立本路线图、R0 基线和 ADR 0019 作为新的权威入口。

## R1：Renderer 的认知反馈回路（已完成）

第一项行为改动只处理已经发生的 Renderer 失败，不扩成通用治理工程：

```text
模型生成内容
  -> 可逆 preview / compile
  -> 把短而充分的真实诊断直接返回模型
  -> 模型自然修改并重试
  -> 成功产物
  -> 唯一一次受控 publish / send
```

目标是把认知试错与外部发送分开。不得再用泛化的
`renderer_execution_failed`、层层状态汇报或继续堆 prompt 约束冒充反馈。首个验收
样例应覆盖未知 LaTeX 命令/缺少宏包等真实错误：模型能看到有用诊断，自行改写表达，
失败草稿不会作为长文本泄漏到群聊。

### R1 当前实施边界（2026-08-29）

- 文档 XeLaTeX 模板加载旧 NoneBot TeX 插件使用的宏包集合，并补充 `esint`；宏包由
  Runtime 固定加载，模型仍不能提交 `\usepackage`、文件读取或 shell escape；
- worker 只把原始日志在内存中归约成固定 schema：错误类别、可选的未知命令和
  `node_id`。原始日志、路径和完整失败内容不跨 Unix socket、不进入 Core 数据库；
- Core 把内容编译错误作为 `renderer_content_error`/HTTP 422 返回调用方，数据库只记
  安全错误码；基础设施失败仍与内容错误分开；
- Nekro bridge `1.1.1` 把这份真实短诊断交回模型现有的有界 Agent loop，自身不再
  维护按请求计数的重试状态。失败草稿不自动降级成长篇普通文本；外部平台发送仍只
  发生在成功产物之后；
- 这一实施复用已有 RenderDocument、artifact、delivery intent 和唯一发送边界，不新增
  第二套 preview/publish API，也不扩成通用 Agent 治理层。

### R1 生产签署（2026-08-30）

- Core/renderer 实现来自 commit `4380154`；移除 bridge 自建重试计数、改由 Nekro
  既有有界 Agent loop 自然迭代的最终 bridge 来自 commit `da8cb17`；
- 44 项聚焦契约测试通过；最终 worker 镜像内的真实隔离探针把 `\oiint` 渲染为
  76,288 字节 PNG，并把未知命令归约为
  `undefined_control_sequence + \\notARealCommand + broken-integral`；
- 生产镜像为 Core
  `sha256:03ced7b1cf94b40f41f409c31674f450f9b5d2f71350e9ed739d78fbeb63e250`、
  document renderer
  `sha256:312915be6a72566414e88bebf6625c43f4488bb1e0c93b14618a12fec0b7560e`、
  LaTeX provider
  `sha256:3c9c5444914664a9c5a0ded10dd6e6138dc5a8166948cc284ccddf329b50bdc1`、
  worker `sha256:78c816bfbb580645d5dcd9beda0cc4d48f5bfefd2446c379c8a51a85375c6af9`；
- worker identity 与 Core render implementation 均为
  `495b98f5a8c5ce034020982a3facc2c27378fb1f6be0b4dfeaf61f7e84248d16`；Provider
  implementation 为 `fd9f2200c1c5ea26c8ebfbc27be20e5ae7671539c998950c513ba848c2f83e62`，
  最新 inventory `8c904098cf812e28f59ac17bd04f2d0f5f4d165c0dc77399125b8ba50478f4e1`
  为 healthy/self-test ok；
- 生产 Core 无发送探针中，未知命令返回 HTTP 422 / `renderer_content_error` 及
  `command=\\notARealCommand,node_id=md001`；`\oiint` 返回 201 和有效 artifact/plan，
  对应 delivery intent 数量严格为 0，因此没有产生平台测试消息；
- Nekro Runtime 仍是 `v2.3.3-superlily.4`，加载 bridge `1.1.1` 后 heartbeat 为
  online，spool 只有 committed、pending=0；Nekro、Core、document renderer、worker、
  Provider 均 running 且 restart count=0；
- 回滚保留旧 Compose 镜像以及
  `/home/justin/nekro/backups/r1-renderer-20260829/` 下 bridge 1.0.0/1.1.0 源码包。
  本次部署同时消除了 R0 记录的旧 Core 镜像无法解析数据库 `0026` 的漂移：生产 Core
  现在为 `0026_history_timeline_export (head)` 且 `alembic check` 无 drift。

至此 R1 完成。它只建立真实、短、有界的 Renderer 认知反馈和成功后唯一发送边界；
没有引入第二套 Runtime、通用工作流或新的平台副作用通道。

## R1.1：引用焦点与引用图片连续性（已完成）

R1.1 修复真实群聊中“引用后召唤莉莉”时引用关系容易被普通历史窗口稀释的问题，不改变
外部副作用边界：

- `run_agent()` 按本次触发消息的精确 message ID 绑定引用，不再把数据库中最新的人类
  消息猜作触发消息；
- 被引用消息与当前请求相邻组成独立 Reply Focus，不占普通 16 条历史和 3200 字符预算，
  因而引用 16 条以前的消息也不会被窗口裁掉；
- 被引用消息拥有独立的 4 张视觉图片预算；普通历史仍保持 1 张，更新的无关图片不能
  挤掉引用图片；
- 引用目标不在本地数据库时，OneBot 适配器在收消息时保存有界快照；若平台也没有提供
  可用内容，则明确输出 unavailable 标记，不再悄悄丢失引用语义；
- Runtime `v2.3.3-superlily.5` / `3b6fb25` 的 lint、typecheck 和 16 项聚焦回归测试通过，
  镜像 `sha256:dd054b342e1d544c1ab74329155665bce31d3a60f39dd5156f48edda06a7877e`
  的 OCI revision/version 与该 commit/tag 一致。
- 生产数据库只读回放确认：一条含 4 张图片的真实引用全部进入 `reply_focus`，普通历史
  图片仍单独保留 1 张；另一条相隔 18 条记录的真实引用中，目标与触发消息均出现在相邻
  Reply Focus。探针只组装上下文，没有调用模型或向 QQ 发送消息。

本项是对当前 Nekro 认知输入连续性的窄修复，不引入通用 router、第二 Runtime 或新的
发送通道。

<a id="r1-2"></a>

## R1.2：节日渲染皮肤（已完成、已上线）

R1.2 为既有 Renderer 提供按时间窗口自动切换的节日展示，独立跟踪范围、交付与验收。
正文和数学仍由 XeLaTeX/Poppler 渲染；装饰为纯图形，不出现文字或顶底分隔线。

- 已上线 16 款：中秋、国庆、重阳、万圣夜、冬至、圣诞、元旦／跨年、除夕、春节初一、
  元宵，以及立春、雨水、惊蛰、春分、清明、谷雨。
- 节气系列仅限春季六节气，现已全部上线；保留已有冬至皮肤。其他季节的节气不列为
  制作计划。后续从传统节日、中外节日和纪念日中挑选，候选清单不代表自动立项。
- 每款恰好使用 24 小时。元旦／跨年窗口为北京时间 2026-12-31 12:00 至
  2027-01-01 12:00；其余已配置皮肤为当天 00:00 至次日 00:00，均左闭右开。
  当前最后一个窗口为 2027-04-20 谷雨，未配置日期使用默认皮肤，不按年自动循环。
- 已于 2026-09-16 完成扩展和跨年窗口调整并部署启用，保留既有多文字字体修复。
  八款新增皮肤的生产成图与批准预览逐一一致；102 项聚焦回归通过，窗口边界、缓存
  到期更新、生产下载与服务健康均已验证。
- 运行观察备注：首个真实节日窗口为 2026-09-25 中秋，尚未观察当天群聊效果。
  已完成的成图和日历验证不冒称当天实测，也不因此把功能继续列为未完成。

最新生产身份与回滚见 [跨年调整记录](NEW_YEAR_WINDOW_20260916.md)及
[部署锁](../deploy/new-year-window.lock.json)；皮肤扩展证据见
[扩展发布记录](FESTIVAL_EXPANSION_20260916.md)，日期与候选见
[节日清单](FESTIVAL_CALENDAR_20260916_20270914.md)。
[初次发布记录](FESTIVAL_RENDERER_20260909.md)保留发布沿革，旧镜像身份不覆盖后续生产改动。

## R2：Cognitive Workspace / World Effect Boundary

2026-09-19：**前两阶段已完成、已上线**，Runtime 为 `2.3.3-superlily.11-r2.1`。
采用 **同群持久共享目录 + 每任务独立临时区**，保留旧群文件，同群执行轮次串行、跨群隔离；
任务过期只清理临时状态。已启用 JSON RPC、执行级授权、安全文件导出及容量保护：
默认单群 256 MiB、单任务 128 MiB、沙盒总量 2 GiB、磁盘预留 2 GiB；总量为监测式软限额，
不是文件系统硬配额，超额停止执行而不删除成果。生产保持 bridge 网络，不启用离线模式。
lint、typecheck、90 项测试及候选镜像 10 轮真实沙盒通过；上线后健康、QQ 重连、采集水位
均已核对。未主动发送测试群消息或调用模型。发布与回滚见 [R2 上线记录](R2_DEPLOYMENT_20260919.md)。
后续 Core 投递扩展、收费工具预算与逐插件权限迁移不在本次前两阶段范围。实现与边界见 Runtime 仓库
[`docs/R2_IMPLEMENTATION.md`](https://github.com/F1Justin/superlily-nekro-runtime/blob/superlily/runtime-v2.3.3/docs/R2_IMPLEMENTATION.md)。

在 R1 的真实实现上概括最小边界：

- Cognitive Workspace 允许私有、可逆的临时文件、执行反馈、局部重试和脚手架；
- World/Core 继续掌握身份、观察事实、长期档案、权限、持久副作用、平台发送和回执；
- 只有拟产生持久或外部影响的结果跨越 effect boundary；
- Core 不要求记录模型每次内部编译、搜索或临时修改，也不把 Runtime session 当作莉莉
  的身份或世界真相。

这一阶段不预设通用多后端接口，不让新的抽象层先于真实能力增长。
R2 是后续 Runtime 能力必须遵守的边界，不是要求在 R3 开始前建设完整的通用工作区
API。R3.1 保持暂停，不作为继续扩建 R2 的理由。

## R3：演进现有 Nekro Runtime

在当前 fork 内逐步形成更自然的 Agent loop：给模型真实、有界的执行反馈，允许它在
可逆工作空间中试错和搭临时脚手架，同时保持外部副作用边界。每次改动由真实失败样本
驱动，并分别观察成功率、回复体验、token/cache 和费用；不以替换 Nekro 为目标。

### R3.1：对话让步与按需 Web Search（因成本暂停）

本项不等待完整 Cognitive Workspace；它只实现 Web Search 所需的最小 R2 切片，并与
搜索 canary 一起验证临时认知材料、世界事实和平台发送之间的边界。

默认关闭的 Runtime 技术 canary 已接通，但全量产品验收尚未完成。OpenRouter server tool
能搜索并返回 URL citation，Nekro 模型组管理界面也已提供默认关闭的开关。将旧 system
prompt 从 371 行压缩到固定的 85 行后，使用当时生产人格快照的单次隔离回放中，
“参与者告知事实”已不再被旧知识反驳；这只是一个有希望的样本，不替代 51 条真实种子
集验收。详细证据见 [`R3_1_CANARY.md`](R3_1_CANARY.md)。搜索接口成功不等于莉莉行为
正确。

2026-08-31 的三次真实搜索请求分别花费 `$0.006383`、`$0.0087685` 和
`$0.0070585`，平均 `$0.007403333`；附近无搜索请求平均 `$0.001973960`，前者约为
后者的 3.75 倍。以 `$0.0070585` 请求为例，11,289 input tokens 产生
`$0.0056445`，138 output tokens 产生 `$0.000414`，Parallel Turbo 搜索本身为
`$0.001`。主要增量来自无缓存命中的完整 system/persona/history 输入，而不只是搜索固定
费用。该成本结构未通过产品门，因此 R3.1 暂停，生产开关保持关闭，不进入小流量 canary
或签署。未完成的 51 条回放及下列验收门只在重新立项后继续，不构成当前排队任务。

生产 Core 自 2026-07-18 起保存了 F1 对莉莉回复添加的事实 reaction。按
`actor=F1 + emoji=👊 + target=Lily response` 去重后可得到 51 条真实纠错目标，其中
50 条能按会话和平台消息 ID 关联回莉莉回复。样本同时包含时效事实、冷门实体、视觉/
OCR、意图理解、工具失败和聊天体验问题，因此 👊 只作为离线评测集入口，不在 Core
中被改写成通用负反馈语义，也不把每条纠错都路由为搜索。

本项提供有界搜索，但不在 Runtime 中判断聊天内容：

- 是否接受参与者告知、何时搜索以及如何表达，由当前人格 prompt 和模型共同决定；
  Runtime 的 system prompt 不再重复这类行为规则；
- 模型组开关开启时，每个初始回答统一获得一次 Web Search 能力；Runtime 不匹配关键词、
  不识别问句，也不把消息分类为“普通请求”或“搜索请求”；
- 后续 sandbox 调试迭代不再提供搜索，避免一次 Agent loop 重复收费；
- 图片中的新闻、人物或产品主张先经过已有视觉输入/OCR 理解，再决定是否搜索；Web
  Search 不替代视觉和引用上下文修复；
- 搜索查询、候选网页和中间归纳属于 Cognitive Workspace 的临时认知材料。网页内容按
  不可信输入处理；它们不自动进入 Core 档案、Memory/RAG 或莉莉的长期世界事实；
- 快速搜索默认只产生最终回复，不固定先发送“我来搜搜”。任何平台进度消息仍由 Core
  的发送边界控制，不能由内部 tool call 顺手产生。

首个生产 canary 使用当前 OpenRouter 模型请求内的 Parallel Turbo server tool，不借用
ChatGPT/Codex 个人套餐，不引入搜索供应商抽象层或第二 Runtime。每次回答最多搜索 1
次，初始上限为 3 个结果、每个结果 1000 字符；实际搜索率的滚动上限为 10%，这是费用
护栏而不是调用目标。Turbo 的中文和中文本地信息能力必须由真实样本证明；失败时明确
表示未核实，不自动升级到更贵的深度搜索。

生产放量前必须通过以下验收门：

1. 将 51 条 👊 样本只读导出为离线回放集，人工区分 `search_required`、
   `search_helpful`、`failed_to_defer`、视觉/OCR、上下文、意图、工具和体验失败；
2. `search_required` 回放的搜索召回率不低于 90%，用户陈述/纠正样本不得因旧模型知识
   继续争辩，普通对话误搜索率不高于 5%；
3. Runtime 分开记录“本次是否提供搜索”“供应商报告的调用次数”和“由 URL citation
   推断的调用次数”，同时记录引擎、端到端延迟、结果字符量、token/cache 和费用；这些
   citation 只用于内部观测与调试，不要求模型在即时聊天中附带 URL 或主动说明来源；
   不得把推断值回填成供应商事实，Core 也不记录每个内部搜索步骤；
4. 搜索超时、无结果、来源冲突和工具不可用均有自然的无搜索降级，不得在未调用工具时
   声称“我查过了”；
5. 分别报告回答正确性、群聊体验、p50/p95 延迟、每千条消息增量费用和缓存变化，再
   决定是否扩大流量；不得只以工具调用成功作为产品验收；
6. 回滚只需从当前 Runtime 请求中移除 server tool 并恢复上一已签署镜像；回滚不修改
   Core 数据、不删除 reaction 事实，也不影响普通 Nekro 回复路径。

本项不包含历史聊天检索、Memory/RAG、后台深度研究、通用浏览器、长期任务或搜索结果
自动写入世界事实；这些能力仍需各自的具体需求和验收门。

### R3.2：真正阅读资料与按需看图（已调查、未实施）

本编号在 2026-09-19 整理时新增，用来归并用户已经提出的 PDF 假阅读、多图遗漏和
按需补看需求，不宣称已经交付，也不授权本次文档整理之外的代码或生产变更。

用户结果：用户发资料并要求分析时，莉莉能够读取正文、指定页或图片，看到真实返回内容后
再决定补读、裁剪、核对或回答；最终仍通过现有消息/渲染器交付，不增加用户插件命令。

调查已确认：生产执行记录曾出现读取 PDF 后未使用提取结果、在同一脚本中直接发送
预先写好的总结；普通脚本正常退出不会自动触发下一轮阅读。现有文本查看工具、3 轮共享
重试预算和约 1,000 字符的默认反馈限制不足以自然支持连续阅读。普通自动带图为 1 张，
引用图片另有 4 张预算；这不是模型只能接受一张图片。

拟实施范围：

1. 提供有来源/页码的文件内容和图片清单，区分“可访问”与“模型已经看过”。
2. 复用现有多模态循环，支持按需读取 PDF 文本、查看指定页/多张图片、局部裁剪；
   由模型决定看什么，不用关键词/意图分类器决定何时查看，不默认把全文和所有图片塞进请求。
3. 沙盒只交文件引用与查看请求，Runtime 校验当前会话权限并直接向模型加入图片；
   不让 Base64 图片经 RPC 返回和 stdout 绕行。视觉输入保留数量、像素、字节与 token 预算。
4. 区分观察续写与代码错误重试，保留有界时间/费用及取消能力；不把预算耗尽后的未读内容
   写成已读，不以脚本打开文件或发送成功代替阅读完成。
5. 用真实失败样本比较回答是否依赖读到的内容，同时检查多轮预算、延迟和费用。

不包括：Luna/双模型、通用文档 Agent、Memory/RAG、后台自动研究、批量聊天媒体归档、
新群务/报名表场景、新发送通道或渲染器重写。按需读取已有附件不等于启用 C0-I 全量归档。

## R4：参考工程研究线

持续研究 Pi、Codex、DSH 和其他 Harness，但产出必须是可验证的设计原则或现有
Runtime 的小改进。不得把研究对象写成集成路线。任何把它们变成 backend、部署依赖或
生产候选的提议，都必须由新的明确 ADR 改变本决策。

## R5：需求触发的产品扩展

多平台、Fumo、Live2D、线下活动、长期任务、历史聊天检索、Memory/RAG、更多账号与
控制面，只在出现具体用户需求时立项。R3.1 只授权有界 Web Search，不为这些相邻能力
提供隐含授权。新工作必须回答 `MANIFESTO.md` 第五条的问题，给出用户可感知结果或新增
数据库可观察事实，并提供精确范围、失败边界和回滚路径。

### R5.1（C0-E）：QQ 平台事实完整性（已实现、已上线）

本项由“应记尽记”的明确数据库需求触发，补齐 OneBot/NapCat 已推送但当前未结构化保存的
身份、群状态、成员权限、请求和群内容事实。它沿用 C0-D 的可靠采集信封和现有名称历史，
不新增平台副作用，不主动轮询好友或群成员，也不修改 ChatExporter。范围、失败边界、
验收门和回滚见 [`C0E_QQ_PLATFORM_FACTS.md`](C0E_QQ_PLATFORM_FACTS.md)。
2026-09-12 只读生产复核：新增范围内 Lily 257 条、Nekro 165 条观测，覆盖成员变动、
名片、群文件、禁言、精华、好友申请和群头衔；421 条采集完整、1 条部分采集。
后续链路排查确认 Nekro 当前 NapCat 的普通消息未填写头衔/等级，独立成员快照已有这些
字段；不能用快照伪造消息时点事实。67 条名片动作已对账为 52 条直接名称关联、10 条同值
去重、5 条清空名片。管理员变动等类型及 Lily 上游字段仍需补齐生产覆盖。
精华缺失/未知操作被默认记为 add 的问题已于 9 月 12 日修复并发布两端，采集恢复及短时
发布验证通过；未制造真实未知精华通知，不将此次发布升级为整阶段签署，详见阶段文档。
单条 `complete` 不等于整个阶段已签署，但不能再将本项概括为未部署或未经过真实运行验证。

### R5.2（C0-F）：QQ 通讯录与群成员快照（生产验收通过）

本项继续落实“应记尽记”，通过默认关闭的周期任务读取好友分类、群扩展信息和群成员列表，
让未发言成员也进入可按 QQ 号检索的关系与名称历史。只保存业务必要字段，主动排除手机号、
邮箱和生日等联系人隐私；范围、失败边界、验收门和回滚见
[`C0F_QQ_DIRECTORY_SNAPSHOTS.md`](C0F_QQ_DIRECTORY_SNAPSHOTS.md)。

### R5.3（C0-G）：OneBot 平台副作用操作账本（生产验收通过）

本项为非消息发送的 OneBot/NapCat 副作用建立调用前/调用后证据，并借用既有 durable event
spool 保证先记账再调用。Core 将两阶段事件聚合为可按实例、会话、接口和时间查询的账本；
超时保持结果不确定，敏感参数不落库，无法可靠关联的后续平台事件不猜测。范围、失败边界、
验收门和回滚见 [`C0G_PLATFORM_API_LEDGER.md`](C0G_PLATFORM_API_LEDGER.md)。

### R5.4（C0-H）：断线补洞（已实现、两端已启用）

本项承接最初“应记尽记”调查的第 4 项，服务 `MANIFESTO.md` 第 2、3 条：机器人断线或
重启后，数据库能够补收平台仍可提供的遗漏消息，并明确标识无法恢复的时间范围。

2026-09-07：两套桥接器已实现默认关闭的有界补采、持久任务与游标；生产 Core 已部署
`0033_history_delivery_receipts`，Nekro 已启用 300 秒小窗口并完成一分钟停机实测：
共同群对照 4/4 补回，含重连等待共补回 8 条，无历史 claim/回复，回执水位追平。
2026-09-08：Lily 自动补采已启用（24 小时回看、50 条/页、100 页/会话）；针对
00:00–07:41 的断档已完成一轮补采，新增 863 条窗口内历史观测；空历史/接口失败保留缺口。
2026-09-12 复核确认后续补采进度及缺口仍在生产库中；Nekro 小窗口断线验收和 Lily
真实断档恢复均属于已取得的生产证据。私聊断线、NapCat 自身离线等场景尚未完成生产验收，
不将小窗口 canary 或补采任务结束视为全部阶段签署，也不把平台全量历史完整性作为已实现承诺。
实现边界、配置和验收记录见 [C0H_QQ_HISTORY_RECOVERY.md](C0H_QQ_HISTORY_RECOVERY.md)。

- 实施前核对实际部署的 OneBot/NapCat 版本及 `get_group_msg_history`、
  `get_friend_msg_history`、`get_recent_contact` 的可用性、分页和时间语义。
- 按实例、会话保存补采水位与任务进度；重连后进行有界、限速、可续跑的历史读取，利用
  已有 native identity 去重，并保留平台发生时间、补采时间和来源。
- 补采仅用于记录历史事实，不触发历史消息的自动回复、命令或其他平台副作用。
- 平台历史不可用、保留期不足、分页中断或身份无法确定时，记录缺口及原因；不得把
  “当前读不到”视为“没有遗漏”，也不承诺恢复平台已删除的事实。
- 验收覆盖断线恢复、重复页、任务中断续跑、限流及不可恢复缺口；确认不会重复写入或
  重放业务行为，且实时采集不因补采受阻。
- 回滚通过关闭补采任务恢复实时采集模式，保留已写入消息、来源、水位和缺口证据。

### R5.5（C0-I）：媒体与合并转发补全（代码已部署、未启用）

本项承接最初调查的第 5 项，服务 `MANIFESTO.md` 第 2 条：数据库能查询合并转发内部节点，
附件在平台临时链接失效后仍有可追溯的元数据与已成功归档的内容引用。

2026-09-08：两套桥接器已实现默认关闭的后台展开/下载、持久任务和有界重试；Core 新增
`0034_qq_media_archive`，保存节点、附件状态、已验证内容哈希及私有稳定引用。自动化和
隔离 PostgreSQL 验证已通过；Core、迁移与两套桥接器已部署，展开/下载仍关闭。
两端 OneBot 现已连接，Lily 历史补采另按 R5.4 启用。媒体真实样本、域名与存储运维仍待验收，
详见 [C0I_QQ_MEDIA_ARCHIVE.md](C0I_QQ_MEDIA_ARCHIVE.md)。
2026-09-12 只读复核：Nekro 两项媒体开关仍为 false，Lily 未配置启用且部署代码默认 false；
`qq_media_archive_items` 与 `qq_media_blobs` 均为 0 条。上线时长不能替代未启用功能的真实样本验证。

- 核对部署版本的 `get_forward_msg`、`get_image`、`get_record`、`get_file`、文件 URL
  获取及下载接口；保存转发节点、顺序、发送者和平台提供的时间，并关联原消息。
- 补齐附件类型、名称、大小、平台标识、内容哈希和稳定存储引用；区分元数据已获取、
  内容已归档、下载失败和资源已失效，不把临时 URL 当作永久归档。
- 下载与展开必须有大小、并发、总存储配额和嵌套深度限制，支持去重及有界重试；鉴权
  凭据和临时签名不进入永久业务记录，缺失的节点、时间和文件内容不伪造。
- 验收覆盖嵌套转发、重复附件、过期链接、超限资源及中断重试，并验证已归档附件不依赖
  原平台 URL 仍可读取；失败原因和来源必须可查。
- 回滚通过关闭展开和下载任务停止新增采集，保留已有元数据、内容与关联证据。

以上五项是同一采集补全方案的推进顺序，分别编号为 R5.1–R5.5，而非 R5.1 内的五个子阶段。
第 1–4 项按已交付范围记为完成/已上线，覆盖不足与已知不可恢复缺口继续保留为备注。
R5.5 代码已部署但未启用；没有全量下载计划，具体元数据/转发或精选附件需求另定范围。
ChatExporter 保持不变，数据库和归档结构继续满足既有导出读取约定。

## 接手与提交规则

新任务先读 `MANIFESTO.md`、本文、`R0_BASELINE.md`、当前 `git status` 和相关 accepted
ADR/合同，再判断它属于 R1–R5 的哪一项。不得从已删除文档、Git 历史中的旧 Phase
顺序或外部 Harness 的功能表自动生成任务。

每次提交只包含本次范围，并在提交后立即推送当前远端分支。
