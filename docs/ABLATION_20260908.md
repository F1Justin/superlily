# 2026-09-08 可逆运行面消融

范围：用户批准上一轮调查建议；不删除业务数据、卷、Provider 容器或历史账本，
不处理独立的 Wolfram worker 故障，不部署 Runtime 工作区中的 R2 改动。

## 变更

- Runtime：未启用插件只注册，不运行初始化；首次热启用先初始化，失败保持禁用并
  清理部分资源；禁用/再次启用保留原回调语义；卸载、重载、关机仅清理已初始化资源。
- Nekro Core Bridge：生产 `AGENT_ENABLED=false`，停止 Core Agent 投递租约轮询。
  Core 的 `AGENT_MODE=off`、`AGENT_PRODUCT_MODE=off` 保持不变。
- 停止 `deploy-status-provider-1`、`deploy-latex-provider-1`，保留其容器、镜像和配置。
  30 秒停止期限后 Docker 发出 SIGKILL（exit 137，非 OOM）；停机前活跃工具尝试为 0。
- Core：artifact 回收独立为 `SUPERLILY_ARTIFACT_REAPER_INTERVAL_SECONDS=30`；
  执行租约回收继续使用 `SUPERLILY_TOOL_REAPER_INTERVAL_SECONDS=1`。
  过期访问校验和 TTL 不变，物理回收允许增加一个扫描周期的延迟。
- Core：canary 模式仅对范围内会话等待第二份观察；范围外仍采集、生成 claim 和回执，
  但不等待。shadow / enforce 的观察等待、灰度防双发与 ACK 条件保持不变。

## 部署身份

Runtime 基础版本仍为 `v2.3.3-superlily.10` / `435febd8e835f8d23105889d94ab1e7226f5667b`。
部署镜像为 `superlily/nekro-agent:2.3.3-superlily.10-ablation.2`，
镜像 ID 为 `sha256:cf6f66a458d7754cdaa50c74493c2e28cfd54529f17e878207a5b861f142920a`。
这是基于生产镜像的两个文件补丁，不是新的已提交 Runtime release。
文件哈希与基础镜像 ID 见 `deploy/nekro-runtime.lock.yml` 的 `local_patch`。
`deploy/Dockerfile.nekro-ablation` 仅复制 `base.py` 与 `collector.py`，不包含 R2。

Core 镜像 ID：`sha256:a98ec3acdd6f60bb7e1752842da2b7ac717e7eafe3affbb6c3bf9d66a9469172`。
部署源码基于当前 HEAD，仅增加本记录中的 claim 与回收调度变更；没有新 migration。

## 验证

- Runtime：72 passed、1 skipped（R2 Docker 专项未运行）；`poe lint` 通过。
- 生产基础镜像的隔离生命周期冒烟通过，不挂生产目录、不联网。
- Core SQLite：690 passed、8 skipped；另将强制 PostgreSQL 的归档契约测试留给 PostgreSQL。
- PostgreSQL 专项：150 passed，涵盖 API / claim / ACK / 回收调度 / settings /
  rendering / artifact / document gateway；独立归档迁移契约 1 passed。
  ORM 与迁移测试使用独立临时数据库，不连接生产。
- 新 Core 镜像 `pip check` 通过，运行容器三个变更模块的 SHA-256 与工作区一致。
- Nekro 最终镜像于 09:23:56 CST 启动，健康，重启计数 0；表情包插件成功注册，
  启动日志没有 Qdrant 初始化失败；生产启用插件仍只有 basic 与 Core Bridge。
- Nekro 更新后的 Core Agent 投递租约请求为 0；更新前 5 分钟为 597 次空请求。
- Core 于 09:23:20 CST 启动，健康，重启计数 0；生产 schema 仍为 `0034_qq_media_archive`。
- 09:23:38–09:24:59（约 81 秒），`render_artifacts` 的全表扫描计数
  `4156852 → 4156855`，`tool_artifacts` 为 `4156537 → 4156540`，各增加 3 次。
  变更前观察约 25 秒各增加 25 次；新配置为 artifact 30 秒、执行租约 1 秒。
- 09:24:59 两端水位分别为 Lily `755738/755738`、Nekro `277684/277684`
  （连续回执/最高观察），均无缺口，且较部署前推进；Nekro WebSocket 已重新连接。
- 停止 latex-provider 后，独立 Renderer 实际返回 HTTP 200、57,488 字节 PNG、
  2048×333，PNG 魔数与响应 SHA-256 均通过校验；没有向 QQ 发送测试消息。
- Core 新实例观察窗口中无 ERROR / Traceback，Agent 投递空轮询为 0；正常 claim 返回 200。
- 09:26:22 最终检查：Lily `755747/755747`、Nekro `277711/277711`；
  两端均 online、各连接 1 个 bot，队列、pending spool、quarantine 均为 0。
  Core / Nekro / Renderer 全部 healthy，重启计数均为 0。
- 临时 PostgreSQL 测试容器及其匿名测试卷已清理；生产卷、历史记录和停机 Provider
  容器均保留。代码未提交或推送，原有文档修改与 Runtime R2 工作区改动未改写。

## 回滚

检查点目录（权限 0700，含私密配置，不得提交）：
`/home/justin/backups/superlily/20260908-ablation-l3r8zA`。

恢复两个 Provider，无需重建：

```bash
sudo docker start deploy-status-provider-1 deploy-latex-provider-1
```

恢复 Nekro：先确认检查点后没有其他配置修改，再恢复旧 bridge 配置和 Compose override。
旧镜像 `superlily/nekro-agent:2.3.3-superlily.10` 已保留。

```bash
sudo cp -a /home/justin/backups/superlily/20260908-ablation-l3r8zA/bridge-config.yaml /home/justin/nekro/plugin_data/Superlily.core_bridge/config.yaml
cp -a /home/justin/backups/superlily/20260908-ablation-l3r8zA/nekro-compose.override.yml /home/justin/superlily/deploy/nekro-compose.override.yml
cp -a /home/justin/backups/superlily/20260908-ablation-l3r8zA/nekro-runtime.lock.yml /home/justin/superlily/deploy/nekro-runtime.lock.yml
sudo docker compose --env-file /home/justin/nekro/.env -f /home/justin/nekro/docker-compose.yml -f /home/justin/superlily/deploy/nekro-compose.override.yml up -d --no-deps --no-build --timeout 60 nekro_agent
```

恢复 Core 镜像（无 schema 变更，不降级数据库）：

```bash
sudo docker image tag superlily/core:pre-ablation-20260908 deploy-lily-core
sudo docker compose --env-file /home/justin/superlily/.env -f /home/justin/superlily/deploy/compose.yml up -d --no-deps --no-build --timeout 60 lily-core
```

源代码保留本次改动时不要再次 `--build`，否则会重新部署新行为。
只回调扫描周期可将 `SUPERLILY_ARTIFACT_REAPER_INTERVAL_SECONDS=1` 写入生产 `.env`
后仅重建 Core；不能通过关闭 artifact 功能回滚频率。

## 观察边界

- Provider 目前为可逆停机，不是永久下线；显式 `docker start` 或 Compose `up`
  仍可能重新启动。观察期内不运行整套 Compose `up`，按服务名操作。
- `document-renderer`、`latex-worker`、Wolfram Provider 保留；原三项停机继续维持。
- Wolfram worker 于本次操作前已退出，重启失败 5 次；故障未在本次范围内处理。
- 一个 `pending` render / `running` attempt 源于 2026-08-10，租约早已过期，
  是操作前已有记录，不在本次消融中改写。
- 扩展验证发现既存问题：历史批量导入器 `_ensure_target` 只接受到 `0031` 的
  schema revision，拒绝当前 `0034_qq_media_archive`。已用变更前 Core 镜像对
  隔离测试库复现；对应源码与测试未修改。归档导入器的 PostgreSQL 测试因此未通过，
  不能把上述专项通过表述为整个 PostgreSQL 套件全绿；未在本轮放宽导入门禁。
- 本次只完成即时回归和短时生产观察，不代表长期对话质量验收；不设置自动删除。
